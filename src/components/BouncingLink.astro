---
interface Props {
	class?: string;
	href: string;
}
const { class: className, href } = Astro.props;
---

<div class:list={["mx-auto animate-bounce", className]}>
	<a
		class="font-jura text-[2rem]/[20px] hover:text-foam underline"
		href={href}
	>
		<slot />
	</a>
</div>
